// This is the test.js it only validates that basic testing works, so should always assert true.
// All actual functions that will be tested will be grouped to have a specific file to improve readability.
// In short, this testing page only tests the testing.
//
//  Where you can find:
// 1) user.spec.js holds tests directly testing the functionality of the code in /src/domain/user.js
// 2) This is text in the Test.js (to be replaced)
// 3) (to be replaced)
// 4) (to be replaced)
// 5) (to be replaced)

// A test investigating if true is true
describe('Basic Test Suite', () => {
  it('should assert true', () => {
    expect(true).toBe(true)
  })
})
